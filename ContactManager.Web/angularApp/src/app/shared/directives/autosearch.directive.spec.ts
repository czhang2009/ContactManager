import { AutosearchDirective } from './autosearch.directive';

describe('AutosearchDirective', () => {
  it('should create an instance', () => {
    const directive = new AutosearchDirective();
    expect(directive).toBeTruthy();
  });
});
